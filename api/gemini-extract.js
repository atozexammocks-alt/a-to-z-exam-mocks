const MODEL = process.env.GEMINI_MODEL || 'gemini-3.8-flash';
const API_KEY = process.env.GEMINI_API_KEY;
const schema = { type:'array', items:{ type:'object', properties:{ qNo:{type:'integer'},qText:{type:'string'},optA:{type:'string'},optB:{type:'string'},optC:{type:'string'},optD:{type:'string'},key:{type:'string'},topic:{type:'string'},targetPath:{type:'string'} }, required:['qNo','qText','optA','optB','optC','optD','key','topic','targetPath'] } };
function send(res,status,body){res.status(status).setHeader('Content-Type','application/json; charset=utf-8').send(JSON.stringify(body));}
module.exports=async(req,res)=>{if(req.method!=='POST')return send(res,405,{error:'POST only'});if(!API_KEY)return send(res,500,{error:'GEMINI_API_KEY is not configured on the server.'});try{const{pageText='',imageDataUrl='',folders=''}=req.body||{};if(!pageText&&!imageDataUrl)return send(res,400,{error:'pageText or imageDataUrl is required.'});const parts=[{text:`You are an exam-paper extraction engine. Extract EVERY MCQ visible in this single PDF page. Preserve mathematical symbols, subscripts, superscripts, Hindi/English text and numbering. Never invent missing text. If an answer key is not visible, return an empty key. Choose the closest library path from [${folders}]. Return only the requested JSON array.`}];if(pageText)parts.push({text:`Native PDF text:\n${pageText}`});if(imageDataUrl){const m=imageDataUrl.match(/^data:(image\/(?:png|jpeg|jpg|webp));base64,(.+)$/i);if(!m)return send(res,400,{error:'Invalid imageDataUrl.'});parts.push({inline_data:{mime_type:m[1].toLowerCase().replace('jpg','jpeg'),data:m[2]}})}const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(MODEL)}:generateContent?key=${encodeURIComponent(API_KEY)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{role:'user',parts}],generationConfig:{responseMimeType:'application/json',responseSchema:schema,temperature:.1}})});const raw=await r.text();if(!r.ok)return send(res,r.status,{error:`Gemini API ${r.status}`,detail:raw.slice(0,4000)});const data=JSON.parse(raw),text=data.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('')||'[]';let items=[];try{items=JSON.parse(text)}catch{return send(res,502,{error:'Gemini returned invalid JSON.'})}return send(res,200,{items:Array.isArray(items)?items:[]})}catch(e){return send(res,500,{error:e?.message||'Unknown extraction error'})}};
